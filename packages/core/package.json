{
  "name": "@patchproof/core",
  "version": "0.1.0",
  "description": "PatchProof deterministic verification core.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./proof.schema.json": "./proof.schema.json"
  },
  "files": [
    "dist",
    "proof.schema.json"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "test": "vitest run"
  },
  "dependencies": {
    "@patchproof/config": "workspace:*",
    "@patchproof/runner": "workspace:*",
    "minimatch": "^10.0.1",
    "zod": "^3.24.1"
  }
}
