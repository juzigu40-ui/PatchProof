# Dependency risk change fixture

This fixture models a fix that also changes dependency metadata. PatchProof reports the
dependency-file risk signal while keeping the deterministic verdict separate.
