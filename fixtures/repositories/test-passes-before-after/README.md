# Test passes before and after fixture

This fixture models a weak test-only change where the reproduction command does not fail on base.
PatchProof should not verify the fix because the claimed failure was not reproduced.
